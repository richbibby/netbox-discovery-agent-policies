webinar_demo:
  selector:
    environment: lab
    purpose: webinar
  policies:
    network_discovery:
      path: policies/network-discovery.yaml
      enabled: false
    device_discovery:
      path: policies/device-discovery.yaml
      enabled: true